export declare const SECOND = 1000;
export declare const MINUTE: number;
export declare const HOUR: number;
export declare const DAY: number;
export declare const WEEK: number;
export declare const MONTH: number;
export declare const YEAR: number;
/**
 * Count the time since date with a given time of unit (in milliseconds).
 * @param time date as a number.
 * @param timeUnit the unit of time in milliseconds.
 * @param singular the word to be appended when the result is singular.
 * @param plural the word to be appended when the result is plural.
 */
export declare function timeUnitSince(time: number, timeUnit: number, singular: string, plural: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in seconds.
 * @param time date.
 * @param plural `seconds` by default
 * @param singular `second` by default
 */
export declare function secondsSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in minutes.
 * @param time date.
 * @param plural `minutes` by default
 * @param singular `minute` by default
 */
export declare function minutesSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in hours.
 * @param time date.
 * @param plural `hours` by default
 * @param singular `hour` by default
 */
export declare function hoursSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in days.
 * @param time date.
 * @param plural `days` by default
 * @param singular `day` by default
 */
export declare function daysSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in weeks.
 * @param time date.
 * @param plural `weeks` by default
 * @param singular `week` by default
 */
export declare function weeksSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in months.
 * @param time date.
 * @param plural `months` by default
 * @param singular `month` by default
 */
export declare function monthsSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date in years.
 * @param time date.
 * @param plural `years` by default
 * @param singular `year` by default
 */
export declare function yearsSince(time: number, singular?: string, plural?: string): {
    count: number;
    formatted: string;
};
/**
 * Count the time since a given date an return the appropriate format with additional information.
 * @param time date.
 * @param _second array of [`singular`,`plural`]
 * @param _minute array of [`singular`,`plural`]
 * @param _hour array of [`singular`,`plural`]
 * @param _day array of [`singular`,`plural`]
 * @param _week array of [`singular`,`plural`]
 * @param _month array of [`singular`,`plural`]
 * @param _year array of [`singular`,`plural`]
 */
export declare function since(time: number, _second?: string[], _minute?: string[], _hour?: string[], _day?: string[], _week?: string[], _month?: string[], _year?: string[]): {
    formatted: string;
    seconds: {
        count: number;
        formatted: string;
    };
    minutes: {
        count: number;
        formatted: string;
    };
    hours: {
        count: number;
        formatted: string;
    };
    days: {
        count: number;
        formatted: string;
    };
    weeks: {
        count: number;
        formatted: string;
    };
    months: {
        count: number;
        formatted: string;
    };
    years: {
        count: number;
        formatted: string;
    };
};
