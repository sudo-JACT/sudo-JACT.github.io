/**
 * Run a callback for each key in a Record.
 * @param callback to be executed.
 * @param object source.
 */
export default function <T extends object>(callback: (key: keyof T, value: T[keyof T], index: number) => void, object: T): void;
