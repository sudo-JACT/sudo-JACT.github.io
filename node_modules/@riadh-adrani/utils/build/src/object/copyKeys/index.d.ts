import { StringWithAutoComplete } from "../../index.js";
/**
 * modifies the target object by copying keys from the source, excluding the provided ones.
 * @param source
 * @param target
 * @param exclude
 */
export default function copyKeys<F extends object, T extends object>(source: F, target: T, ...exclude: Array<StringWithAutoComplete<keyof F>>): void;
