/**
 * creates a new object with only the given keys.
 * @param object source.
 * @param keys keys to preserve
 */
export default function pick<T extends object, K extends keyof T>(object: T, ...keys: Array<keyof T | K>): Pick<T, K>;
