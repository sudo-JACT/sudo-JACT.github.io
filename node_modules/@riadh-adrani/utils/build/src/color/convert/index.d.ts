import { ColorType } from "../index.js";
/**
 * convert given `H S L` values to `R G B`
 * @param h hue
 * @param s saturation
 * @param l lightness
 */
export declare function hslToRgb(h: number, s: number, l: number): [number, number, number];
/**
 * convert given `R G B` values to `H S L`
 * @param r red
 * @param g green
 * @param b blue
 */
export declare function rgbToHsl(r: number, g: number, b: number): [number, number, number];
/**
 * convert given `R G B` values to `#rrggbbaa`
 * @param r red
 * @param g green
 * @param b blue
 * @param a alpha
 */
export declare function rgbToHex(r: number, g: number, b: number, a?: number): string;
/**
 * convert a given css declaration to another one.
 *
 * supported types : `HEX` | `RGB` | `HSL`
 *
 * @param color source
 * @param to target type
 */
export declare function convertColor(color: string, to: ColorType): string;
