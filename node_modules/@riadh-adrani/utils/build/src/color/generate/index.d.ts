import { Palette, ColorType } from "../index.js";
/**
 * generates a safe color that could be paired with the given one,
 * as a text or background color.
 *
 * @param color source
 * @param light color for dark colors
 * @param dark color for light colors
 */
export declare function generateContrastSafeColor(color: string, light?: string, dark?: string): string;
/**
 * generates a complementary color.
 * @param color source
 * @param type result color type
 */
export declare function generateComplementaryColor(color: string, type?: ColorType): string;
/**
 * generates a tonal palette.
 * @param color
 * @param type output colors type
 * @see https://m3.material.io/styles/color/the-color-system/key-colors-tones#6bdb9471-b70d-42c9-8ace-76743c1fff13
 */
export declare function generateColorTonalPalette(color: string, type?: ColorType): Palette;
/**
 * generate the same color with a modified opacity.
 * @param color source
 * @param opacity opacity
 * @param type output color type
 */
export declare function changeColorOpacity(color: string, opacity: number, type?: ColorType): string;
